                                                                                                                      │
│ 智教未来 AI 教育平台 -- 完整开发规划（三阶段）                                                                       │
│                                                                                                                      │
│ Context                                                                                                              │
│                                                                                                                      │
│ 侧边栏导航已实现（CHG-20260416-02 待验证）。现在需要规划前端+后端的完整开发路径，将 Mock 数据替换为真实 LinkAI       │
│ 调用，并为4个 AI 模块（互动课件/命题/组题/教案）实现差异化 UI 和用户体系。                                           │
│                                                                                                                      │
│ 已发现的 BUG（必须在阶段一首先修复）                                                                                 │
│                                                                                                                      │
│ 1. 路由双重前缀：backend/app/api/__init__.py:10 有 prefix="/api"，main.py:108 又加了 prefix="/api"，导致实际路径为   │
│ /api/api/chat/generate（404）                                                                                        │
│ 2. AI_FUNCTIONS 未导入：chat.py:145 引用了 AI_FUNCTIONS，但定义在 generations.py:53，未导入（Mock 模式下 NameError） │
│ 3. LinkAI API 接口不匹配（backend/app/services/linkai.py）：                                                         │
│   - 代码使用 POST /v1/workflows/execute + workflow_code + input                                                      │
│   - 实际 LinkAI 接口是 POST /v1/workflow/run + app_code + args                                                       │
│   - 响应格式也不同：实际返回 { success: true, data: { output_text: "..." } }                                         │
│   - 必须修正为真实接口，否则阶段一无法正常调用                                                                       │
│                                                                                                                      │
│ 重要约束                                                                                                             │
│                                                                                                                      │
│ 1. 作业批改中台：独立 Flask 项目（端口 3005），接口 POST                                                             │
│ /correct，在本机运行。本次开发只预留接口、数据字段、超链接占位，不实现对接，后期好接入。                             │
│ 2. 结果渲染类型：AI 生成结果不只有 Markdown 文本，互动游戏/互动课件类型可能返回 HTML 文件，需要用 iframe             │
│ 渲染。TaskResult 需要区分 content_type（markdown/html）并支持两种渲染方式。                                          │
│ 3. LinkAI 是同步 API：/v1/workflow/run 直接返回结果（或支持 stream 流式），没有 task_id                              │
│ 轮询机制。当前代码的"提交→轮询"架构需要改为"提交→等待（或流式）"。后端改为接收请求后同步调用                         │
│ LinkAI，通过流式转发给前端实现实时进度。                                                                             │
│ 4. 生成结果页三栏布局：参考飞象老师 UI，生成结果页应为左栏（侧边栏）+ 中栏（内容展示+操作栏+追问输入框）+ 右栏（HTML │
│  文件实时预览 iframe）。                                                                                             │
│                                                                                                                      │
│ 参考文档                                                                                                             │
│                                                                                                                      │
│ - docs/智教未来-项目规划v0.5.md -- 原始项目规划（技术栈已从 Node.js 改为                                             │
│ Python/FastAPI，其余数据模型和架构设计仍适用）                                                                       │
│ - docs/linkai接口.md -- LinkAI 真实 API 文档（/v1/workflow/run、/v1/chat/memory/completions、TTS/ASR）               │
│ - docs/飞象web/UI参考分析.md -- 飞象老师 UI 截图分析（三栏布局、生成进度、卡片样式）                                 │
│                                                                                                                      │
│ ---                                                                                                                  │
│ 阶段一：打通 AI 生成闭环                                                                                             │
│                                                                                                                      │
│ 目标：后端 Mock → LinkAI 真实调用，前端提交任务 + 轮询状态 + 结果展示。                                              │
│                                                                                                                      │
│ CHG-1.1：修复后端路由 Bug                                                                                            │
│                                                                                                                      │
│ 文件：backend/app/api/__init__.py                                                                                    │
│ 改动：移除 prefix="/api"（main.py 注册时已有），修正路径从 /api/api/... → /api/...                                   │
│                                                                                                                      │
│ CHG-1.2：linkai.py 修正为真实 LinkAI API                                                                             │
│                                                                                                                      │
│ 文件：backend/app/services/linkai.py                                                                                 │
│ 改动：                                                                                                               │
│ 1. 接口地址：/v1/workflows/execute → /v1/workflow/run                                                                │
│ 2. 请求体：{ workflow_code, input } → { app_code, args }                                                             │
│ 3. 删除 query_workflow_status() 方法（LinkAI 无此接口）                                                              │
│ 4. submit_workflow 改为同步调用（或 stream 流式），直接返回 { data: { output_text } }                                │
│ 5. 从 config.py 读取的 workflow code 对应 LinkAI 的 app_code                                                         │
│                                                                                                                      │
│ CHG-1.3：chat.py Mock 替换为 LinkAI 真实调用                                                                         │
│                                                                                                                      │
│ 文件：backend/app/api/routes/chat.py                                                                                 │
│ 改动：                                                                                                               │
│ 1. 删除 _tasks 内存字典、process_generation_task 后台任务、TaskStatus 轮询机制                                       │
│ 2. POST /api/chat/generate 改为同步调用 LinkAI /v1/workflow/run，直接返回结果（或 SSE 流式转发）                     │
│ 3. 删除 GET /api/chat/status/{task_id} 端点（LinkAI 无异步轮询）                                                     │
│ 4. 保留 GET /api/chat/tasks 用于历史记录（阶段三改为查数据库）                                                       │
│ 5. 扩展 GenerateRequest：添加可选的模块专属参数字段（animation_type/duration/difficulty/question_types/question_coun │
│ t/exam_type/total_score/exam_duration/lesson_type/class_size）                                                       │
│ 6. 扩展 TaskResult：添加 ai_function、content_type（markdown/html）和 structured_data 字段                           │
│ 7. TaskResult 增加 html_url 字段：当 content_type=html 时存储 HTML 文件路径                                          │
│                                                                                                                      │
│ CHG-1.4：新建前端 API 服务层                                                                                         │
│                                                                                                                      │
│ 新建：frontend/src/lib/api.ts                                                                                        │
│ - submitGenerate(params) → POST /api/chat/generate（同步返回结果）                                                   │
│ - submitGenerateStream(params) → POST /api/chat/generate（SSE 流式，后续可选）                                       │
│ - getTaskList(params) → GET /api/chat/tasks                                                                          │
│ - getHotGenerations(params) → GET /api/generations/hot                                                               │
│ - getCategories() → GET /api/categories                                                                              │
│                                                                                                                      │
│ CHG-1.5：新建前端类型定义                                                                                            │
│                                                                                                                      │
│ 新建：frontend/src/lib/types.ts                                                                                      │
│ - AIFunction, GenerateRequest, GenerateResponse, TaskResult, GenerationCard                                          │
│                                                                                                                      │
│ CHG-1.6：新建前端生成 Hook                                                                                           │
│                                                                                                                      │
│ 新建：frontend/src/hooks/useGenerate.ts                                                                              │
│ - generate() 调用 API，isLoading 状态管理                                                                            │
│ - 无轮询，直接等待结果返回                                                                                           │
│ - 返回 result/error/isLoading                                                                                        │
│                                                                                                                      │
│ CHG-1.7：改造 ChatInput 组件                                                                                         │
│                                                                                                                      │
│ 文件：frontend/src/app/components/ChatInput.tsx                                                                      │
│ - 接收 aiFunction prop，onSubmit 回调替代内部 setTimeout Mock                                                        │
│ - 提交时调用 useGenerate.generate()                                                                                  │
│                                                                                                                      │
│ CHG-1.8：新建 GenerationResult 组件（三栏布局）                                                                      │
│                                                                                                                      │
│ 新建：frontend/src/app/components/GenerationResult.tsx                                                               │
│ - 参考 UI 参考文档，采用三栏布局：                                                                                   │
│   - 中栏：标题 + 标签 + 摘要 + 内容区（Markdown 或 HTML iframe）+ 追问输入框 + 操作栏                                │
│   - 右栏（条件渲染，content_type=html 时显示）：HTML 文件实时预览区 + 预览控制栏（文件名/全屏/下载）                 │
│ - 操作栏预留"提交到作业批改中台"按钮（disabled + tooltip "即将上线"）                                                │
│ - 标题 + 标签 + 摘要 + 内容区 + 操作栏（下载/收藏按钮占位）                                                          │
│                                                                                                                      │
│ CHG-1.9：提取 GenerationPage 共用组件 + 改造4个页面                                                                  │
│                                                                                                                      │
│ 新建：frontend/src/app/components/GenerationPage.tsx                                                                 │
│ 修改：frontend/src/app/(main)/animation/page.tsx + question/exam/lesson 的 page.tsx                                  │
│ - GenerationPage 封装 Header + ChatInput + CategoryTabs + UGCGrid + Result                                           │
│ - 各页面只需传入 aiFunction prop                                                                                     │
│                                                                                                                      │
│ CHG-1.10：Header 组件动态化                                                                                          │
│                                                                                                                      │
│ 文件：frontend/src/app/components/Header.tsx                                                                         │
│ - 接收 title/subtitle props，根据 aiFunction 动态显示                                                                │
│                                                                                                                      │
│ CHG-1.11：添加前端环境变量                                                                                           │
│                                                                                                                      │
│ 新建：frontend/.env.local                                                                                            │
│ NEXT_PUBLIC_API_BASE=http://localhost:8000/api                                                                       │
│                                                                                                                      │
│ 阶段一验证                                                                                                           │
│                                                                                                                      │
│ - curl http://localhost:8000/api/chat/tasks 返回空列表（路由双重前缀已修复）                                         │
│ - curl -X POST http://localhost:8000/api/chat/generate 成功调用 LinkAI 并返回结果（后端日志显示调用链路）            │
│ - 前端 /animation 提交任务 → loading 状态 → 完成后看到 Markdown/HTML 结果                                            │
│ - HTML 类型结果：右侧预览栏 iframe 正确渲染                                                                          │
│ - 4个页面均可正常提交和展示                                                                                          │
│                                                                                                                      │
│ ---                                                                                                                  │
│ 阶段二：4个模块差异化 UI                                                                                             │
│                                                                                                                      │
│ 目标：每个模块有专属参数表单和结构化结果展示。                                                                       │
│                                                                                                                      │
│ CHG-2.1~2.4：新建4个模块专属表单                                                                                     │
│                                                                                                                      │
│ 新建：                                                                                                               │
│ - frontend/src/app/components/forms/AnimationForm.tsx -- 动画类型/时长                                               │
│ - frontend/src/app/components/forms/QuestionForm.tsx -- 题型多选/难度/数量                                           │
│ - frontend/src/app/components/forms/ExamForm.tsx -- 试卷类型/总分/时长/题型比例                                      │
│ - frontend/src/app/components/forms/LessonForm.tsx -- 课时类型/课时数/班额/教材版本                                  │
│                                                                                                                      │
│ CHG-2.5：改造 GenerationPage                                                                                         │
│                                                                                                                      │
│ 文件：frontend/src/app/components/GenerationPage.tsx                                                                 │
│ - 根据 aiFunction 动态渲染对应的 Form 组件（替代统一的 ChatInput）                                                   │
│                                                                                                                      │
│ CHG-2.6：新建4个结构化结果展示组件                                                                                   │
│                                                                                                                      │
│ 新建：                                                                                                               │
│ - results/AnimationResult.tsx -- 概览卡片 + 教学目标 + 场景列表（时间/画面/旁白/交互）。当 content_type=html         │
│ 时，主体区域用 iframe 渲染 HTML 文件（互动游戏/教学动画），同时保留概览信息                                          │
│ - results/QuestionResult.tsx -- 题目卡片列表 + 答案解析（可折叠）                                                    │
│ - results/ExamResult.tsx -- 试卷信息栏 + 大题分组 + 参考答案Tab + 评分标准Tab                                        │
│ - results/LessonResult.tsx -- 教学目标 + 课时Tab列表 + 板书设计 + 教学反思                                           │
│                                                                                                                      │
│ CHG-2.7：后端结果结构化解析                                                                                          │
│                                                                                                                      │
│ 新建：backend/app/services/result_parser.py                                                                          │
│ - 将 LinkAI 返回的 Markdown 解析为结构化 JSON                                                                        │
│ - 按 ai_function 分发到不同的解析器                                                                                  │
│ - 阶段二完成后需根据 LinkAI 实际返回格式调试                                                                         │
│                                                                                                                      │
│ 阶段二验证                                                                                                           │
│                                                                                                                      │
│ - 4个页面显示各自专属表单                                                                                            │
│ - 提交后专属参数透传到 LinkAI（后端日志确认）                                                                        │
│ - 生成结果以结构化卡片/分组形式展示，而非纯 Markdown                                                                 │
│                                                                                                                      │
│ ---                                                                                                                  │
│ 阶段三：用户体系 + 数据持久化                                                                                        │
│                                                                                                                      │
│ 目标：PostgreSQL 持久化、JWT 认证、历史记录、收藏分享、积分系统。                                                    │
│                                                                                                                      │
│ 数据模型（PostgreSQL）                                                                                               │
│                                                                                                                      │
│ users             -- 用户表（phone, password_hash, nickname, avatar_url, points）                                    │
│ generation_tasks  -- 生成任务表（user_id, ai_function, content_type, html_url, 请求参数, LinkAI结果, 状态）          │
│ favorites         -- 收藏表（user_id, task_id）                                                                      │
│ point_logs        -- 积分记录表（user_id, amount, type, description）                                                │
│ shares            -- 分享表（user_id, task_id, share_code, view_count）                                              │
│ -- 作业批改中台预留字段（generation_tasks 表内）：                                                                   │
│ --   correction_status  VARCHAR(20) DEFAULT NULL  -- 待批改/批改中/已完成（占位）                                    │
│ --   correction_id      VARCHAR(100) DEFAULT NULL -- 批改中台返回的作业ID（占位）                                    │
│                                                                                                                      │
│ -- 作业批改相关表（占位，后期接入时创建）：                                                                          │
│ -- classes               -- 班级表                                                                                   │
│ -- students              -- 学生表                                                                                   │
│ -- homework_assignments  -- 作业布置表                                                                               │
│ -- homework_submissions  -- 作业提交表                                                                               │
│ -- correction_records    -- 批改记录表（annotations, output_urls, points_cost）                                      │
│                                                                                                                      │
│ CHG-3.1：ORM 基础设施                                                                                                │
│                                                                                                                      │
│ 新建：backend/app/models/base.py, backend/app/db/session.py                                                          │
│ - SQLAlchemy 声明基类 + 异步引擎 + 会话工厂                                                                          │
│                                                                                                                      │
│ CHG-3.2：ORM 模型定义                                                                                                │
│                                                                                                                      │
│ 新建：backend/app/models/ 下 user.py, task.py, favorite.py, point_log.py, share.py                                   │
│                                                                                                                      │
│ CHG-3.3：认证服务                                                                                                    │
│                                                                                                                      │
│ 新建：backend/app/services/auth.py, backend/app/api/routes/auth.py, backend/app/api/deps.py                          │
│ - POST /api/auth/register / POST /api/auth/login / GET /api/auth/me                                                  │
│ - get_current_user 依赖注入                                                                                          │
│                                                                                                                      │
│ CHG-3.4：chat.py 任务持久化                                                                                          │
│                                                                                                                      │
│ 文件：backend/app/api/routes/chat.py                                                                                 │
│ - 内存字典 → PostgreSQL 读写                                                                                         │
│ - 添加 Depends(get_current_user) 认证保护                                                                            │
│                                                                                                                      │
│ CHG-3.5~3.8：业务路由                                                                                                │
│                                                                                                                      │
│ 新建：                                                                                                               │
│ - routes/history.py -- GET/DELETE /api/history（我的创作）                                                           │
│ - routes/favorites.py -- GET/POST/DELETE /api/favorites                                                              │
│ - routes/shares.py -- POST /api/shares/{task_id}, GET /api/shares/{share_code}                                       │
│ - routes/points.py -- GET /api/points/balance, GET /api/points/logs                                                  │
│                                                                                                                      │
│ CHG-3.9~3.11：前端认证                                                                                               │
│                                                                                                                      │
│ 新建：frontend/src/lib/auth.ts, hooks/useAuth.ts, contexts/AuthContext.tsx                                           │
│ 新建：frontend/src/app/login/page.tsx, register/page.tsx                                                             │
│ - AuthProvider 包裹根布局                                                                                            │
│ - 未登录访问受保护页面 → 跳转 /login                                                                                 │
│                                                                                                                      │
│ CHG-3.12：Sidebar 用户信息动态化                                                                                     │
│                                                                                                                      │
│ 文件：frontend/src/app/components/Sidebar.tsx                                                                        │
│ - 从 AuthContext 读取 nickname/points                                                                                │
│ - 新增"我的创作"/"我的收藏"菜单项                                                                                    │
│                                                                                                                      │
│ CHG-3.13：新建历史/收藏页面                                                                                          │
│                                                                                                                      │
│ 新建：frontend/src/app/(main)/history/page.tsx, favorites/page.tsx                                                   │
│                                                                                                                      │
│ CHG-3.14~3.15：Redis + MinIO                                                                                         │
│                                                                                                                      │
│ 新建：backend/app/services/cache.py, backend/app/services/storage.py                                                 │
│ - Redis：热门UGC缓存、任务状态缓存                                                                                   │
│ - MinIO：头像上传、结果导出文件                                                                                      │
│                                                                                                                      │
│ 阶段三验证                                                                                                           │
│                                                                                                                      │
│ - alembic upgrade head 成功建表                                                                                      │
│ - 注册 → 登录 → 访问受保护页面 → 生成任务 → 历史记录可见 → 收藏 → 分享链接可访问                                     │
│                                                                                                                      │
│ ---                                                                                                                  │
│ 执行顺序                                                                                                             │
│                                                                                                                      │
│ 阶段一（核心闭环）             阶段二（差异化 UI）          阶段三（用户体系）                                       │
│ ──────────────────           ──────────────            ──────────────                                                │
│ 1.1 修路由Bug ──┐                                                                                                    │
│ 1.2 linkai.py ──┤                                                                                                    │
│ 1.3 chat.py ────┤                                                                                                    │
│ 1.4 API服务层 ──┤──→ 2.1~2.4 四个Form ──→ 3.1 ORM基础                                                                │
│ 1.5 类型定义 ───┤──→ 2.5 GenerationPage → 3.2 模型定义                                                               │
│ 1.6 生成Hook ───┤──→ 2.6 四个Result ───→ 3.3 认证服务                                                                │
│ 1.7 ChatInput ──┤──→ 2.7 结果解析器 ───→ 3.4 任务持久化                                                              │
│ 1.8 结果组件 ───┤                          → 3.5~3.8 业务路由                                                        │
│ 1.9 GenerationPage┤                          → 3.9~3.11 前端认证                                                     │
│ 1.10 Header ────┤                          → 3.12~3.15 其他                                                          │
│ 1.11 环境变量 ──┘                                                                                                    │
│                                                                                                                      │
│ 当前执行：阶段一（从 CHG-1.1 开始）                                                                                  │
╰──────────────────────────────────────────────────────────────────────────────────────────────────────────────────────╯

● 我来开始实施阶段一。先读取关键文件了解当前状态，同时创建任务追踪